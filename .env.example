# Supabase/PostgreSQL connection
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_SSL_MODE=require
DB_SCHEMA=pulse

# Redis (optional - leaderboard cache)
# if empty, caching is disabled and all reads go to postgres
REDIS_URL=redis://localhost:6379

# Supabase Auth
SUPABASE_JWT_SECRET=